#!/bin/bash
PORT=${1:-8888}
open "http://localhost:"$PORT
docker run -it --rm -p $PORT:8888 -v ${PWD}/src/:/home/jovyan/work --ipc=host jupyter/r-notebook:latest jupyter notebook --no-browser --ip=0.0.0.0 --allow-root --NotebookApp.token=
